[project]
name = "demos-hw-stm32"
version = "0.0.0"
requires-python = ">=3.14"
dependencies = [
    "playwright",
]

[dependency-groups]
dev = [
    "mypy",
    "pylint",
    "pytest",
    "ruff",
]

[tool.uv]
# Demo/teaching repo: not an installable package.
package = false
